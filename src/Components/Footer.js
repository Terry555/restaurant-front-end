import React from 'react';

function Footer(props) {

    return(
      <footer className="footer">
        <main>
        Restaurant Name
        </main>
      </footer>
    );

}

export default Footer;
